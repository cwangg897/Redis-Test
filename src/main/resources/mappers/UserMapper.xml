<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cwg.test.mapper.UserMapper" >

    <insert id="insertUser" parameterType="com.cwg.test.dto.UserDto">
        INSERT INTO USER(id, password, level)
        VALUES (#{id}, #{password}, #{level})
    </insert>

    <select id="findById" resultType="com.cwg.test.dto.UserDto">
        SELECT id, password, level
        FROM USER
        WHERE id = #{id}
    </select>

    <select id="isExistUser" resultType="boolean">
        SELECT EXISTS (SELECT id FROM USER WHERE id = #{id})
    </select>





</mapper>